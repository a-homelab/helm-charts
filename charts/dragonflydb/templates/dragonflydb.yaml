apiVersion: dragonflydb.io/v1alpha1
kind: Dragonfly
metadata:
  name: {{ include "dragonflydb.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "dragonflydb.labels" . | nindent 4 }}
  annotations:
    {{- include "dragonflydb.annotations" . | nindent 4 }}
spec:
  labels:
    {{- include "dragonflydb.selectorLabels" . | nindent 4 }}

  replicas: {{ .Values.replicaCount }}
  image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
  serviceAccountName: {{ include "dragonflydb.serviceAccountName" . }}

  {{- if and .Values.snapshot.enabled (eq .Values.snapshot.type "s3") }}
  args:
    - --s3_endpoint={{ .Values.snapshot.s3.endpoint }}
    - --s3_use_https={{ not .Values.snapshot.s3.insecure | toString }}
    {{- with .Values.args }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- else }}
  {{- with .Values.args }}
  args:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- end }}

  {{- if and .Values.snapshot.enabled (eq .Values.snapshot.type "s3") }}
  env:
    - name: AWS_ACCESS_KEY_ID
      valueFrom:
        secretKeyRef:
          name: "{{ include "dragonflydb.fullname" . }}-snapshots"
          key: AWS_ACCESS_KEY_ID
    - name: AWS_SECRET_ACCESS_KEY
      valueFrom:
        secretKeyRef:
          name: "{{ include "dragonflydb.fullname" . }}-snapshots"
          key: AWS_SECRET_ACCESS_KEY
    - name: AWS_REGION
      value: us-east-1
    {{- with .Values.args }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- else }}
  {{- with .Values.env }}
  env:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- end }}

  {{- if .Values.tls.enabled }}
  tlsSecretRef:
    name: "{{ include "dragonflydb.fullname" . }}-server-tls"
  {{- end }}
  
  {{- if .Values.auth.enabled }}
  authentication:
    {{- if eq .Values.auth.mode "password" }}
    passwordFromSecret:
      {{- with .Values.auth.password.existingSecret }}
      name: {{ .name | quote }}
      key: {{ .key | quote }}
      {{- else }}
      name: "{{ include "dragonflydb.fullname" . }}-password"
      key: {{ .Values.auth.password.secretKey | quote }}
      {{- end }}

    {{- else if eq .Values.auth.mode "tls" }}
    {{- if and .Values.tls.enabled .Values.auth.clientCaCertSecret }}
    clientCaCertSecret:
      name: {{ .Values.auth.clientCaCertSecret.name }}
    {{- else }}
    {{- fail ".Values.tls.enabled and .Values.auth.clientCaCertSecret must both be set when .Values.auth.mode is 'password'" }}
    {{- end }}

    {{- else }}
    {{- fail printf "Invalid value for .Values.auth.mode: '%s'. Must be one of 'password', 'tls'" .Values.auth.mode }}

    {{- end }}
  {{- end }}

  {{- if .Values.snapshot.enabled }}
  snapshot:
    cron: {{ .Values.snapshot.cron | quote }}

    {{- if eq .Values.snapshot.type "s3" }}
    dir: "s3://{{ include "dragonflydb.fullname" . }}-snapshots"
    {{- else if eq .Values.snapshot.type "pvc" }}
    {{- with .Values.snapshot.pvc }}
    persistentVolumeClaimSpec:
      {{- toYaml . | nindent 6 }}
    {{- else }}
    {{- fail ".Values.snapshot.type must be one of: `s3`, `pvc`" }}
    {{- end }}
  {{- end }}

  {{- with .Values.podAnnotations }}
  podAnnotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}

  resources:
    {{- toYaml .Values.resources | nindent 4 }}
  
  {{- with .Values.affinity }}
  affinity:
    {{- toYaml . | nindent 4 }}
  {{- end }}

  {{- with .Values.tolerations }}
  tolerations:
    {{- toYaml . | nindent 4 }}
  {{- end }}

  {{- if gt (.Values.replicaCount | int) 1 }}
  {{- with .Values.topologySpreadConstraints }}
  topologySpreadConstraints:
    {{- range . }}
    - {{- toYaml . | nindent 6 }}
      {{- if not .labelSelector }}
      labelSelector:
        matchLabels:
          {{ include "dragonflydb.selectorLabels" $ | nindent 10 }}
      {{- end }}
    {{- end }}
  {{- end }}
  {{- end }}
